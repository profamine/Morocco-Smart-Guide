name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        # On commente ou supprime temporairement cette ligne pour détruire le mauvais cache
        # cache: 'npm'

    - name: Install Dependencies
      run: npm ci

    # --- ÉTAPE AJOUTÉE ET CORRIGÉE ---
    - name: Create .env file
      run: |
        echo "VITE_GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env
        echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
        # Vous pouvez ajouter d'autres variables si nécessaire ici, exemple :
        # echo "VITE_API_URL=https://votre-api.com" >> .env

    - name: Build Web Application
      run: npm run build

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '21' # Changé de 17 à 21

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Capacitor Sync
      run: npx cap sync android

    - name: Build APK with Gradle
      run: |
        cd android
        chmod +x ./gradlew
        ./gradlew assembleDebug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: morocco-smart-guide-debug-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk